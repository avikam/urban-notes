{
  "name": "urban-notes",
  "version": "1.0.0",
  "description": "send notes to urban-notes",
  "main": "index.js",
  "scripts": {
    "build": "gulp",
    "run": "osascript -l JavaScript dist/urban_notes.jxa",
    "clean-run": "rm -fr dist && npm run build && npm run run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.19.3",
    "@babel/preset-env": "^7.19.3",
    "browserify": "^17.0.0",
    "globby": "^13.1.2",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-sourcemaps": "^3.0.0",
    "node-fetch": "^2.6.1",
    "vinyl-buffer": "^1.0.1",
    "vinyl-source-stream": "^2.0.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "jsdom": "^20.0.1"
  }
}
