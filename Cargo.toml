[package]
name = "todo_sync"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1"
rocket = { version = "0.5.0-rc.2", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
